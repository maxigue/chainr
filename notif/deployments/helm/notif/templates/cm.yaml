apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chainr-notif.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "chainr-notif.name" . }}
    helm.sh/chart: {{ include "chainr-notif.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
data:
  config.yaml: |
    port: 80
